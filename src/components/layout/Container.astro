---
export interface Props {
  as?: 'div' | 'section' | 'header' | 'footer' | 'main' | 'nav';
  class?: string;
}

const { as: Tag = 'div', class: extra = '' } = Astro.props;
---

<Tag class:list={['mx-auto w-full max-w-page px-5 md:px-20', extra]}>
  <slot />
</Tag>
